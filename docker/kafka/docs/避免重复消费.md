避免异常消费

保证消息有序消费
1. 同一个会话的消息发送到同一个分区，rebalance的时候不会导致重复消费

保证消息不重复消费
1. 为了提高吞吐，项目选择异步commit offset，所以要先关闭自动提交
2. 业务处理有异常抛出则不提交offset
3. 消费端幂等（redis分布式锁+mongodb消息状态检查）
             业务处理时先通过分布式锁将消息标记为已消费（后续如果业务处理失败就回滚），下次消息如果发现是重复消息则忽略后续处理步骤；
             如果redis挂了，就检查mongo这条消息是否被消费过。消费过就不处理了。